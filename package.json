{
  "name": "paytrax",
  "version": "1.0.0",
  "description": "Browser-based payroll management system for small businesses",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration"
  },
  "keywords": ["payroll", "small-business", "pwa", "vanilla-js"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@vitest/browser": "^1.5.0",
    "@vitest/ui": "^1.5.0",
    "@vitest/coverage-istanbul": "^1.5.0",
    "playwright": "^1.42.0",
    "vitest": "^1.5.0"
  }
}
